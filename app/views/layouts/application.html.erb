<!DOCTYPE html>
<html>
  <head>
    <title>EcommerceApplicaion</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <a href='/users/sign_out'><span>Sign Out</span>
    <% else %>
    <% end %>   
    <%= yield %>
  </body>
</html>
